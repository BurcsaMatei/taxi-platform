🚀 Pornire server local de dezvoltare – vezi modificările live pe portul implicit 3000:
npm run dev

👀 Mod de preview local – rulează proiectul în mod development pe portul 3002:
npm run preview

🏗️ Build complet pentru producție – generează fișiere optimizate:
npm run build

▶️ Rulare server de producție pe portul 3002 (după build):
npx next start -p 3002

♻️ Update „safe” – actualizează doar JSON-ul galeriei pe baza imaginilor:
npm run gen:gallery

🆕 Rescriere completă – regenerează JSON-ul galeriei de la zero:
npm run gen:gallery:fresh

🧹 Curățare intrări lipsă – șterge imaginile lipsă din JSON:
npm run gen:gallery:prune

🔄 Combinat – regenerează complet + curăță:
node scripts/genGalleryJson.js --fresh --prune

📄 Export documentație simplă (fiecare fișier separat în /docs):
npm run docs:export

📄 Export documentație componente combinate (cod + styles într-un singur MD):
npm run docs:export:components

🌐 Generare site static de documentație (MD → HTML navigabil):
npm run docs:site

🧹 Curățare manuală a build-ului Next.js (șterge folderul .next):
Remove-Item -Recurse -Force .next
